- [x] generalize mini stat component and fragment for librariesStats selection
- [x] can probably generalize a EntityConvenientSortMenu to replace BooksSortAndDisplayMenu/SeriesSortAndDisplayMenu
- [ ] reconsider stats, showing effectively the same thing for both series and books is fine for now but i think it can be better.
    - [ ] completedSeries for server/[id]/series/index.tsx?
    - [ ] does time matter for series search? feels less relevant than for library/books (at-a-glance for lib vs more specific for total time)
- [x] use generalized componenets i'll def wind up making (ofc ofc) for series books
    - [x] add a `Series Position` sort option
- [x] rm old filter ui components
- [x] make local library consistent with these patterns introduced
- [x] libraries/[id]/index.tsx (library series)
- [ ] formalize patch into pr for expo
- [x] useGridOrListItemSize
    - maybe useGridOrList that returns size and common things like ItemSeparatorComponent etc?
- [x] move list/ and grid/ into layout/
- [x] fix paddingHorizontal for lists (prolly just need to be 0)
- [ ] kill smartListLayout, use new patterns
- [ ] infoItems in ListRowItem
    - i have as react node now, i think i _could_ do something more structured like InfoItem type that I construct
      in the component?
- [x] BookListItem is already taken :sob: figure that out smh
- [x] shared gradients between ListRowItem and GridImageItem
- [ ] numberOfReads ListRowItem?
