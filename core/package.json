{
  "name": "@stump/core",
  "version": "0.0.0",
  "description": "",
  "keywords": [],
  "license": "MIT",
  "scripts": {
    "prisma": "cargo prisma",
    "setup": "cargo prisma generate",
    "check": "cargo check",
    "build": "cargo build --release",
    "fmt": "cargo fmt --all  --manifest-path=./Cargo.toml --",
    "benchmarks": "cargo test --benches",
    "typegen": "cargo test --package stump --bin stump -- types::tests --nocapture",
    "codegen": "pnpm prisma generate && cargo test --package stump --bin stump -- types::tests --nocapture",
    "test": "cargo test"
  },
  "devDependencies": {
    "@types/node": "^17.0.45",
    "axios": "^0.27.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.3"
  }
}