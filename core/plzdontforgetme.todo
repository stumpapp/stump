- [ ] rm finished_reading_sessions
    - [ ] might do this early to catch lingering swaps needed
- [ ] replace reading_sessions with reading_sessions_v2
- [x] refactor kobo sync
    - [x] rewrite apply_reading_session_joins, pull latested v2 sess
    - [x] finished_reading_session_count, count (distinct readthorugh_number) where did_complete
    - [x] finished_reading_session_last_completed_at -> max updated_at where did_complete
    - [x] find other things i am sure i need to fix just took a glance
- [x] refactor opds
    - [x] bunch 'o reading_session references to eval in entry.rs, publication.rs, entity.rs
- [ ] cli account.rs, refacotr to transfer v2 sessions now
- [x] write migrations once i am CERTAIN these changes are solid, fuck if i am doing it before then
    - [ ] will need to figure out backfill
    - [ ] remove postfixes
- [ ] drizzle migrations, too
- [x] refactor series completion toggle
- [x] refactor SeriesFinishedCountLoader
- [ ] figure out refactor for registered_reading_device
    - [ ] rename reading_device, registered is redundant
    - [ ] consider optional `email` for send-to
- [ ] refactor SeriesStats
- [ ] find other things i am sure i need to fix just took a glance
- [ ] it would be REALLY convenient to try and think about some kind of e2e testing... because this kind of sweeping change is particularly hard to test and i've mostly been porting without testing
    - [ ] verify changes to opds aren't broken
- [ ] search and address TODO(v2-sessions) comments
