{
	"presets": [
		"@babel/preset-env",
		[
			"@babel/preset-react",
			{
				// Note: This is to allow React to automatically import the JSX runtime, i.e.
				// we don't need to have `` in every file.
				"runtime": "automatic"
			}
		],
		"@babel/preset-typescript"
	]
}
