export default function UpdateAvailableOverlay() {
	// TODO: would ping server to check if update is available
	// 1. send client version to server
	// 2. server responds with latest versions of client and server, and if update is available
	//    for either

	return null;
}
