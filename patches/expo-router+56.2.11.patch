diff --git a/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/index.js b/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/index.js
index fee7cd8..62b0996 100644
--- a/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/index.js
+++ b/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/index.js
@@ -138,6 +138,7 @@ function convertStackToolbarSubmenuMenuPropsToRNHeaderItem(props) {
         return undefined;
     }
     const sharedProps = (0, shared_1.convertStackHeaderSharedPropsToRNSharedHeaderItem)(props);
+    const xcassetName = (0, shared_1.extractXcassetName)(props);
     const actions = react_1.Children.toArray(props.children).filter((child) => (0, children_1.isChildOfType)(child, exports.StackToolbarMenuAction) || (0, children_1.isChildOfType)(child, exports.StackToolbarMenu));
     const item = {
         type: 'submenu',
@@ -162,7 +163,10 @@ function convertStackToolbarSubmenuMenuPropsToRNHeaderItem(props) {
         item.destructive = props.destructive;
     }
     // TODO: Add elementSize to react-native-screens
-    if (sharedProps.icon) {
+    if (xcassetName) {
+        item.icon = { type: 'xcasset', name: xcassetName };
+    }
+    else if (sharedProps.icon) {
         if (sharedProps.icon.type === 'sfSymbol') {
             item.icon = sharedProps.icon;
         }
@@ -206,12 +210,13 @@ const StackToolbarMenuAction = (props) => {
     // TODO(@ubax): Handle image loading using useImage in a follow-up PR.
     const icon = typeof props.icon === 'string' ? props.icon : undefined;
     const source = typeof props.icon !== 'string' ? props.icon : undefined;
-    return ((0, jsx_runtime_1.jsx)(native_1.NativeToolbarMenuAction, { ...props, icon: icon, source: source, image: props.image, imageRenderingMode: props.iconRenderingMode }));
+    return ((0, jsx_runtime_1.jsx)(native_1.NativeToolbarMenuAction, { ...props, icon: icon, source: source, xcassetName: props.xcassetName, image: props.image, imageRenderingMode: props.iconRenderingMode }));
 };
 exports.StackToolbarMenuAction = StackToolbarMenuAction;
 function convertStackToolbarMenuActionPropsToRNHeaderItem(props) {
-    const { children, isOn, unstable_keepPresented, icon, ...rest } = props;
+    const { children, isOn, unstable_keepPresented, icon, xcassetName, ...rest } = props;
     const sharedProps = (0, shared_1.convertStackHeaderSharedPropsToRNSharedHeaderItem)(props);
+    const derivedXcassetName = xcassetName ?? (0, shared_1.extractXcassetName)(props);
     const item = {
         ...rest,
         description: props.subtitle,
@@ -223,7 +228,10 @@ function convertStackToolbarMenuActionPropsToRNHeaderItem(props) {
     if (unstable_keepPresented !== undefined) {
         item.keepsMenuPresented = unstable_keepPresented;
     }
-    if (sharedProps.icon) {
+    if (derivedXcassetName) {
+        item.icon = { type: 'xcasset', name: derivedXcassetName };
+    }
+    else if (sharedProps.icon) {
         if (sharedProps.icon.type === 'sfSymbol') {
             item.icon = sharedProps.icon;
         }
diff --git a/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/types.d.ts b/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/types.d.ts
index b9b9a81..e4e1012 100644
--- a/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/types.d.ts
+++ b/node_modules/expo-router/build/layouts/stack-utils/toolbar/StackToolbarMenu/types.d.ts
@@ -295,6 +295,13 @@ export interface StackToolbarMenuActionProps {
      */
     subtitle?: string;
     hidden?: boolean;
+    /**
+     * The name of an asset catalog image to use as the icon.
+     * Use this instead of `icon` when your icon is an `.xcasset` custom symbol (not an SF Symbol).
+     *
+     * @platform ios
+     */
+    xcassetName?: string;
 }
 export interface NativeToolbarMenuActionProps extends LinkMenuActionProps {
     /** @platform android */
diff --git a/node_modules/expo-router/build/layouts/stack-utils/toolbar/shared.js b/node_modules/expo-router/build/layouts/stack-utils/toolbar/shared.js
index 186be4b..da75656 100644
--- a/node_modules/expo-router/build/layouts/stack-utils/toolbar/shared.js
+++ b/node_modules/expo-router/build/layouts/stack-utils/toolbar/shared.js
@@ -56,15 +56,14 @@ function convertStackHeaderSharedPropsToRNSharedHeaderItem(props, isBottomPlacem
             return undefined;
         }
         // Bottom placement xcasset uses native xcasset type
-        if ('xcasset' in iconComponentProps && isBottomPlacement) {
+        if ('xcasset' in iconComponentProps) {
             return {
                 type: 'xcasset',
                 name: iconComponentProps.xcasset,
             };
         }
-        // Unified image path for src and xcasset (non-bottom)
-        if ('src' in iconComponentProps || 'xcasset' in iconComponentProps) {
-            const source = 'src' in iconComponentProps ? iconComponentProps.src : { uri: iconComponentProps.xcasset };
+        if ('src' in iconComponentProps) {
+            const source = iconComponentProps.src;
             const explicitRenderingMode = 'renderingMode' in iconComponentProps ? iconComponentProps.renderingMode : undefined;
             const effectiveRenderingMode = explicitRenderingMode ??
                 props.iconRenderingMode ??
